"small function which are created during runtime called lambda functions"
"Dont't need return statement to output from this fucntion"
"Used where we cannot use regular def functions"

#creating basic lambda function
a = lambda x,y : x*y
print(a(5,4))